# C Language

```C
#include <stdio.h>

int main() {
        printf("something");
    return 0;
}

```
<details>
<summary>Counter Statement</summary>
 
``` c

    for (int i = 0; i <= length; i++) {
        
    }

    while () {
        
    }

```
</details>

<details>

<summary>Conditional Statement</summary>

``` c

    if (condition = true) {
        // code block. do something if True or will  execute/ evaluate if true
    } else if (condition) {
        
    } else {
        
    }
    
    
    
    if (condition == true) {
        
    } else {
        
    }
    
    
    
    if (condition = true) {
        
    }
    
    condition ? true : false ;
    
	switch (condition) {
		case 1:
			
			break;
		case 2:
			
			break;
		case 3:
			
			break;
		default:
			
			break;
	}
  

```
 
</details>



<details>
<summary>Shorthand</summary>
	
``` C
 
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <ctype.h>


int main() {
    int  age = 22 ;	
    int *ptr = &age ;
    int _age = *ptr ;
    
    printf("number is : %d", age);
    
    && 
    
    printf("\n\n\n\n\n\n\n\n\n\n");
    return 0;
	}
	
	
   							// meaningful name
   							// error through   =  compilation faild
   							// execution speed = slow নাকি fast ?
   							// compile = convert করা (.c → .exe/.out file এ convert করা )
   							// execute = 
   							// Run Time Error  = compiler check করতে পারবে না 
   							// debugging && Testing 
   							// 
   							// 
   							// 
   							// ==== ipe ==== 
   							// inheritance 
   							// polymorphism 
   							// encapsulation 
   							// 
   							// 
   							// === Function === লাফাপ্র 
   							// built-in Function = Library Function 
   							// user defined Function 
   							// recursive	 Function 
   							// 
   							// বিল্টিন = উজার্ডিফাইন্ড 
   							// built-in নাকি user defined 
   							// 
   							// operator ====
   							// 
   							// PMDM মডিউল ভাগশেষ 
   							// এলজিয়েন =  ইকুয়াল্টু  
   							// এন্ডোরনট  = নটি কুয়াল্টু 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							// 
   							//  // code block. do something if True or will  execute if true            
    
    



/*=========================================
=======  if, else if, else  ====== i ======
=========================================== */ 
    if (condition = true) {
        
    } else if (condition) {
        
    } else {
        
    } 

/*=========================================
=============  if  & else  ======= ii =====
=========================================== */ 
    if (condition = true) {
        
    } else {
        
    }  
    
/*=========================================
=============  only if ========= if ======
=========================================== */ 
    if (condition = true) {
        
    } 
    
/*=========================================
===========    only else   =================
=========================================== */ 
    else {
        
    } 
/*=========================================
===========    print   ===================
=========================================== */ 
    printf("something"); 
    printf("something \n"); 
    scanf("%format_specifier",&Variable ); 
    Hello Buddy, What's up? 
    
    
    
    int x <= 545 ? true : false ; // ternary 
    
/*=========================================
===========    past    =================
=========================================== 

"  " meaning  in bangla                = i
elaborate in bangla                    = e               
" translate in English                 = t
Hello Buddy, What's up?                = h
what is the difference of "   &   " ?  = d

*/
```

</details>
